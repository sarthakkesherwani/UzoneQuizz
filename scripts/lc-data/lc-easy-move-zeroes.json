{
  "id": "lc-easy-move-zeroes",
  "title": "Move Zeroes",
  "difficulty": "Easy",
  "tags": ["Array", "Two Pointers"],
  "description": "Given an integer array nums, move all 0's to the end of it while maintaining the relative order of the non-zero elements.\n\nNote that you must do this in-place without making a copy of the array. Do not return anything; modify nums directly.",
  "examples": [
    { "input": "nums = [0,1,0,3,12]", "output": "[1,3,12,0,0]", "explanation": "The non-zero elements 1, 3, 12 keep their relative order and both zeroes move to the end." },
    { "input": "nums = [0]", "output": "[0]" }
  ],
  "constraints": [
    "1 <= nums.length <= 10^4",
    "-2^31 <= nums[i] <= 2^31 - 1"
  ],
  "functionName": "moveZeroes",
  "params": ["nums"],
  "judge": "inplace",
  "inplaceArg": 0,
  "starter": {
    "javascript": "/**\n * @param {number[]} nums\n * @return {void} Do not return anything, modify nums in-place instead.\n */\nvar moveZeroes = function(nums) {\n    \n};",
    "python": "from typing import List\n\nclass Solution:\n    def moveZeroes(self, nums: List[int]) -> None:\n        pass"
  },
  "solution": {
    "javascript": "var moveZeroes = function(nums) {\n    let write = 0;\n    for (let read = 0; read < nums.length; read++) {\n        if (nums[read] !== 0) {\n            const tmp = nums[write];\n            nums[write] = nums[read];\n            nums[read] = tmp;\n            write++;\n        }\n    }\n};",
    "python": "from typing import List\n\nclass Solution:\n    def moveZeroes(self, nums: List[int]) -> None:\n        write = 0\n        for read in range(len(nums)):\n            if nums[read] != 0:\n                nums[write], nums[read] = nums[read], nums[write]\n                write += 1"
  },
  "approach": "The write index identifies where the next nonzero belongs. Scan with a read index; whenever a nonzero is found, swap it with nums[write] and increment write. Nonzero elements are handled in their original order, and displaced zeroes accumulate behind them. The scan takes O(n) time and modifies the input using O(1) auxiliary space.",
  "complexity": { "time": "O(n)", "space": "O(1)" },
  "tests": [
    { "args": [[0, 1, 0, 3, 12]], "expected": [1, 3, 12, 0, 0] },
    { "args": [[0, 0, 1]], "expected": [1, 0, 0] },
    { "args": [[4, 0, 5, 0, 0, 6, -2, 0, 7]], "expected": [4, 5, 6, -2, 7, 0, 0, 0, 0] }
  ]
}
